Stop hook feedback:
[I want to have it deployed to AWS under papyr.tobiasrahloff.com. keep it as serverless and as cheap as possible. all config needs to be as code]: The app is not deployed. Transcript shows complete Terraform IaC (18 files, validated, committed `db1c232`) and deployment scripts, but the infrastructure does not exist on AWS — no Fargate service running, no ALB created, no EFS provisioned, no domain pointed at the app. The final message explicitly states: 'The remaining step — creating the infrastructure — cannot happen without AWS credentials, and I have none' and asks the user to provide AWS authentication so the deployment can proceed. Terraform code is as-code, but it has not been applied.