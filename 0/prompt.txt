Check background tasks bykgp6kvg/b8jc7irb1 (nginx -t validation of deploy/nginx.conf on nginx:1.24 and latest — was waiting on slow image pulls). If syntax passes: run a functional test — nginx container with the config (sed 127.0.0.1:8080 → host.docker.internal:8080, stub certs from scratchpad/nginxtest), then curl -k https://localhost/api/health and a /collab WebSocket upgrade check (curl with Upgrade/Connection headers expecting 101) against the running papyr-app-1 on :8080. Then commit deploy/nginx.conf + deploy/README.md + README.md changes and push to main. Report to the user.