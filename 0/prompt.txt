Check release run 30009543777 (v0.2.0; app image Build&push step running >1h under QEMU). If completed: verify multi-arch (docker buildx imagetools inspect ghcr.io/trahloff/aldine-app:latest), gh release view v0.2.0, native arm64 quickstart test (compose up scratchpad/qs, health, down -v), report in German. If still running past ~2h total or failed: the fix is cross-compilation — apps/server/Dockerfile build stages should use FROM --platform=$BUILDPLATFORM so the JS build runs natively and only the runtime image is per-arch (server deps are pure JS); implement that, test locally with docker buildx build --platform linux/amd64,linux/arm64, commit, re-tag v0.2.0 (delete+recreate tag) or tag v0.2.1.