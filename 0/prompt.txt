<task-notification>
<task-id>a5528c454ac701b45</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-502/-Users-rahloff-projects-Papyr/94ff4a6b-9a22-4b9e-ba63-714ccb35a98b/tasks/a5528c454ac701b45.output</output-file>
<status>completed</status>
<summary>Agent "Angle B: removed-behavior audit" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>I have everything needed. Points 3 (JSON cache) and 5 (dead-code removal) are clean — no demonstrable regression. Here are the confirmed findings.

[{"file":"apps/server/src/index.ts","line":66,"summary":"SIGTERM now commits only branches with open Yjs docs, dropping the old unconditional 'commit every project's main' safety net. Working-tree changes made through non-collab paths that don't commit themselves — PUT /api/projects/:id/file (used by web binary/image upload, FileTree.tsx:99) and /references/add (routes.ts:593), both of which only writeFile + refreshBranchDocsFromDisk — are no longer captured at shutdown when their branch has no open doc.","failure_scenario":"User uploads an image via the file tree (PUT /file, base64) to a project, then closes the editor so Hocuspocus unloads all its docs. No autocommit fires afterward (no doc edit). A deploy/scale-in sends SIGTERM. flushAllDocs() returns no project/branch for that project (zero open docs), so commitAll is never called for it. The uploaded asset stays uncommitted: on ephemeral container storage it is lost; on the live EFS+PG deploy it lingers uncommitted until the next explicit commit/push, absent from git history/PRs meanwhile. The old code's listProjects()+commitAll(p.id,'main') committed it."},{"file":"apps/server/src/db/json.ts","line":146,"summary":"usage.json shape changed from {userId:{month,seconds}} to {userId:{month:seconds}} with no migration. getUsageSeconds now does usage()[userId]?.[month]; against old data usage()[userId] is {month:'2026-07',seconds:N}, so ?.['2026-07'] is undefined and returns 0 — silently resetting every JSON-backend user's current-month compile quota on upgrade (live deploy is Postgres, unaffected; self-hosters are).","failure_scenario":"A JSON-backed self-host deployment upgrades past this commit mid-month. A user who had consumed their monthly typeset budget (old usage.json: {u:{month:'2026-07',seconds:99999}}) now reads getUsageSeconds→0, so overQuota()→false and they get a full fresh quota. addUsageSeconds then writes a new '2026-07' key alongside the orphaned month/seconds cruft, accumulating from 0. Quota enforcement is bypassed for the transition month."},{"file":"apps/server/src/collab.ts","line":74,"summary":"The deleted disk read-back (fs.readFileSync(abs)==text) is replaced by an in-memory lastWritten hash that is empty after onLoadDocument seeds a doc from disk. So the first writeDocToDisk after a load rewrites byte-identical content, bumping mtime (defeating the latexmk-incremental goal the comment claims to preserve) and firing a spurious scheduleAutoCommit. Separately, refreshBranchDocsFromDisk's content===null branch (line 207, file deleted by merge/reset/pull) returns without lastWritten.delete, so a later flushBranchDocs no longer re-materializes the file the old read-back would have.","failure_scenario":"User opens a project and clicks Compile without editing. flushBranchDocs iterates the freshly-loaded docs; lastWritten has no entry, so each identical file is rewritten and its mtime bumped right before latexmk runs, forcing a full rebuild instead of the incremental one the removed read-back enabled (recurs on the first compile after every process restart, since lastWritten is in-memory)."},{"file":"apps/server/src/db/pg.ts","line":21,"summary":"jsonb() does JSON.stringify(v).replace(/\\u0000/g,'') on the already-escaped JSON. A real NUL (escaped to \\u0000) is stripped correctly, but user text containing the literal 6 chars backslash-u-0000 becomes \\\\u0000 after stringify; the regex then matches the \\u0000 tail and removes it, leaving a dangling backslash — invalid jsonb → the same 500 the function was added to prevent (or, with trailing text, a shifted valid escape → silent corruption). Reachable via project name (writeMeta) or review-comment text (saveComments).","failure_scenario":"A user saves a review comment whose text is exactly the literal string backslash-u-0000. saveComments→jsonb(list): JSON.stringify escapes the backslash to \\\\u0000, the regex strips \\u0000 leaving \\\", producing an unterminated JSON string; Postgres rejects the jsonb and the write 500s."}]</result>
<usage><subagent_tokens>94883</subagent_tokens><tool_uses>16</tool_uses><duration_ms>491255</duration_ms></usage>
</task-notification>