A session-scoped Stop hook is now active with condition: "Since Prism got bought by OpenAI and Overleaf is becoming clunkier and clunkier and even more expensive, I'm considering building out a slim open source LaTeX collaboration platform that essentially is an Overleaf copy but with modern UI and user experience experience and much more responsive. Please do deep research. Also look at Bibi, B-I-B-B-Y, which is apparently now the top shelf paid option when it comes to features and independently build a fully functioning prototype that is defined via Docker containers. So it should be hostable via docker acceptance criteria is LaTeX compilation LaTeX collaboration in real time good sync with multiple branch support and modular skill or plugin system because we want to extend it with similar features that Bibi is offering later on. Independently work in hypothesis, planning, implementation, test loops. Always test via Playwright UI tests. I will be asleep for the next at least 15 hours. So you need to fully autonomous and independently iterate. Do not stop. Iterate until you reach the acceptance criteria. And independently do product management in sub-agents with new UX improvements that you encounter along the way. It also needs to have a native zotero integration. 

Stretch-goals: LaTeX templates, everything that bibby can do, etc. 



Very important: UI and UX (apple style, simple, straighforward, easy to use) is paramount. Use subagents to review, qa, and test your own work.

I already have an example latex paper that you can use for testing here: https://github.com/trahloff/paper-gsaas-2026. you can use gh cli for interacting with GitHub but I also already cloned it at /Users/rahloff/projects/paper-gsaas-2026". Briefly acknowledge the goal, then immediately start (or continue) working toward it — treat the condition itself as your directive and do not pause to ask the user what to do. The hook will block stopping until the condition holds. It auto-clears once the condition is met — do not tell the user to run `/goal clear` after success; that's only for clearing a goal early.