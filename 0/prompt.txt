<task-notification>
<task-id>adb29a537b846bf0d</task-id>
<tool-use-id>toolu_01FLQ4PHCcxRRHkieXREkfnx</tool-use-id>
<output-file>/private/tmp/claude-502/-Users-rahloff-projects-Papyr/94ff4a6b-9a22-4b9e-ba63-714ccb35a98b/tasks/adb29a537b846bf0d.output</output-file>
<status>completed</status>
<summary>Agent "Angle F: AWS IaC + deploy scripts" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>I have enough to finalize. Ruling out two non-issues I verified: `tsx` is a runtime `dependency` (line 12-18 block), so `npx tsx` in the server Dockerfile resolves from installed deps — not a bug; and the JSON store writes atomically (temp + rename), so the rolling-deploy window risks lost-updates but not file corruption.

Here are the concrete defects, most severe first.

[{"file":"deploy/aws/.github/workflows/deploy-aws.yml","line":47,"summary":"CD workflow builds linux/amd64 images (lines 47 and 57) but the ECS task definition pins cpu_architecture to ARM64 (var default + terraform.tfvars line 7), so Fargate/Graviton cannot run them.","failure_scenario":"Operator triggers the 'Deploy to AWS' workflow to ship a new version. It pushes amd64 images and force-new-deployment. New ARM64 tasks fail to start with an image/platform-manifest mismatch; the deployment_circuit_breaker (ecs.tf:154) rolls back to the previous task set. `aws ecs wait services-stable` then returns success on the rolled-back OLD image, so the workflow reports green while the new code never actually deploys."},
{"file":"deploy/aws/push-images.sh","line":10,"summary":"REGION defaults to eu-central-1, but the committed terraform.tfvars sets region=eu-west-1 and deploy.sh neither exports AWS_REGION nor passes the region it computed to ./push-images.sh (deploy.sh:49).","failure_scenario":"Fresh clone; operator follows the README (`export AWS_PROFILE=papyr; ./deploy.sh`) without exporting AWS_REGION. Terraform provisions ECR/ECS in eu-west-1 (from tfvars). deploy.sh then runs push-images.sh, which builds REGISTRY=&lt;acct&gt;.dkr.ecr.eu-central-1.amazonaws.com and `docker buildx build --push` fails with 'name unknown: repository papyr-server does not exist' (the repos live in eu-west-1). set -e aborts the one-command deploy with infra half-up and no images pushed."},
{"file":"deploy/aws/teardown.sh","line":14,"summary":"teardown.sh hardcodes `terraform destroy`, but deploy.sh explicitly supports OpenTofu (`command -v terraform || command -v tofu`, deploy.sh:17).","failure_scenario":"A user who deployed with tofu (no `terraform` binary on PATH) runs ./teardown.sh to avoid ongoing AWS cost. After confirming 'destroy', the script dies with 'terraform: command not found' before tearing anything down; the EFS/ALB/Fargate stack keeps billing until they discover the mismatch and run `tofu destroy` manually."},
{"file":"deploy/aws/efs.tf","line":32,"summary":"EFS access points are created root-owned (owner_uid/gid 0; /data 0755, /secrets 0700, lines 32-34 and 49-51) with no posix_user, which only works because both Dockerfiles set no USER and run as root — an implicit, undocumented coupling. The shell-escape-enabled TeX compiler also runs as root.","failure_scenario":"A later hardening pass adds `USER node` (uid 1000) to apps/server/Dockerfile for least-privilege. The container can no longer write /data (0755 root-owned) and cannot even read /secrets (0700 root-owned), so the server fails all git/datastore writes and Zotero-key reads at runtime with EACCES — a non-obvious break because the Terraform looks independent of the image's user. Separately, running the compiler as root weakens the shell-escape sandbox."},
{"file":"deploy/aws/ecr.tf","line":7,"summary":"Repos are image_tag_mutability=MUTABLE and everything deploys under the single mutable tag `latest` (variables.tf image_tag default), while the lifecycle policy keeps only the last 5 images by push order.","failure_scenario":"A bad build is pushed as `latest` and rolled out. There is no immutable prior tag/digest to roll back to via `image_tag`, and after a few more pushes the previously-good image is expired by the keep-last-5 lifecycle policy, so recovery requires rebuilding old source rather than re-pointing to a known-good digest."},
{"file":"deploy/aws/push-images.sh","line":2,"summary":"Stale header comment says the script builds 'linux/amd64 (Fargate is x86_64)', directly contradicting line 11 which defaults PLATFORM to linux/arm64 to match the ARM64 task def.","failure_scenario":"A maintainer debugging the amd64/ARM64 mismatch (finding #1) reads push-images.sh line 2, trusts the comment, and 'fixes' the workflow toward amd64 or flips cpu_architecture to X86_64 — reintroducing an arch mismatch instead of resolving it. Pure doc rot, but it actively misleads on the exact axis that is already fragile."},
{"file":"deploy/aws/ecs.tf","line":163,"summary":"deployment_minimum_healthy_percent=100 / maximum=200 with a single EFS /data mounted RW by both old and new tasks means every rolling deploy has a brief 2-task window writing the same JSON datastore; the inline comment only reasons about the Yjs collab seed race, not datastore lost-updates.","failure_scenario":"During a redeploy, old and new tasks briefly coexist and both read-modify-write the same project meta.json on EFS. Writes are atomic per file (temp+rename), so no corruption, but a concurrent edit to project metadata during the overlap can be lost (last rename wins). Low-probability and seconds-long, but unlike the collab seed race it isn't autosaved back — the losing update is simply gone. Set 0/100 to avoid the overlap if that matters."}]</result>
<usage><subagent_tokens>74914</subagent_tokens><tool_uses>32</tool_uses><duration_ms>415549</duration_ms></usage>
</task-notification>