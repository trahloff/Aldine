<task-notification>
<task-id>a1036ee546ff3d815</task-id>
<tool-use-id>toolu_01KdiSrn2bKArTrKZpdbArjT</tool-use-id>
<output-file>/private/tmp/claude-502/-Users-rahloff-projects-Papyr/94ff4a6b-9a22-4b9e-ba63-714ccb35a98b/tasks/a1036ee546ff3d815.output</output-file>
<status>completed</status>
<summary>Agent "Angle D: JS/TS pitfalls" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Confirmed empirically: `jsonb({body:"...\u0000..."})` (literal backslash-u-0000 text) produces `{"body":"handle \ in the parser"}` — a lone backslash that `JSON.parse` rejects ("Bad escaped character"), which Postgres would 500 on, while the actual-NUL case strips cleanly. All findings verified.

[{"file":"/Users/rahloff/projects/Papyr/apps/server/src/db/pg.ts","line":21,"summary":"jsonb() strips serialized \\u0000 with /\\\\u0000/g, which also corrupts any value containing the literal text backslash-u-0000, producing invalid JSON that Postgres rejects.","failure_scenario":"A review comment / project-meta / connection field whose text contains the literal 6-char sequence backslash-u-0000 (e.g. a reviewer writing about a NUL byte in a code listing — realistic on a LaTeX/CS-paper platform) serializes to \\\\u0000; the regex removes the trailing u0000 and leaves a lone backslash that escapes the next character. Empirically JSON.parse fails ('Bad escaped character'), so Postgres rejects the jsonb param with 'invalid input syntax for type json' -&gt; saveComments/writeMeta/setConnection 500, and every subsequent comment save for that project fails. The intended actual-NUL fix still works; this is a regression only for the literal-text case."},
{"file":"/Users/rahloff/projects/Papyr/apps/server/src/db/json.ts","line":47,"summary":"read() returns the live object stored in the write-through cache; auth.ts mutates user.salt/hash on that reference before updateUser() persists, so the cache diverges from disk on write failure and aliases across concurrent callers.","failure_scenario":"auth.changePassword/resetPassword call db().getUser() (JsonStore returns this.users()[id] — the cached object itself), then set user.salt/user.hash in place BEFORE updateUser() writes. If write() throws (ENOSPC/EIO/rename fail) the in-memory cache already holds the new hash while disk keeps the old -&gt; the process authenticates the new password but a restart silently reverts it. Two concurrent change-password calls for the same user share one object, so the first caller's updateUser persists the second caller's hash, losing a password change while returning success."},
{"file":"/Users/rahloff/projects/Papyr/apps/server/src/routes.ts","line":384,"summary":"GET /output sets Content-Length from statSync then streams via createReadStream; the .papyr-out PDF is rewritten by latexmk on every recompile, so size can change or the file can vanish between stat and stream open.","failure_scenario":"A viewer fetches the PDF during a recompile: Content-Length comes from the pre-recompile stat but bytes come from the new file. If the new PDF is smaller the socket hangs waiting for the promised bytes (or the client gets a truncated PDF); if larger, extra bytes are dropped. If the artifact is deleted between statSync (succeeds) and createReadStream opening it, the stream emits an async ENOENT after send() has already committed the content-length header, so Fastify returns a 500/aborted response instead of the intended 404 from the catch block."},
{"file":"/Users/rahloff/projects/Papyr/apps/server/src/db/json.ts","line":146,"summary":"The JSON usage store switched from {userId:{month,seconds}} to {userId:{[month]:seconds}} with no migration of existing usage.json.","failure_scenario":"After deploying this change, getUsageSeconds reads usage()[userId]?.[month] against the old shape whose keys are literally 'month' and 'seconds' -&gt; undefined ?? 0, so every existing user's accumulated current-month compile usage resets to 0 and their quota is silently wiped. addUsageSeconds then writes the new month key alongside the stale month/seconds keys, leaving mixed garbage in the record. (Postgres path was already per-month, so only JSON self-hosters are hit.)"},
{"file":"/Users/rahloff/projects/Papyr/apps/server/src/collab.ts","line":51,"summary":"lastWritten Map gains an entry per doc on every write but is only removed by evictDoc (explicit file delete) and refresh-when-changed — never when Hocuspocus unloads an idle document, so it grows unbounded.","failure_scenario":"On a long-running multi-project server, each file ever edited leaves a permanent doc-name -&gt; sha1 entry after all clients disconnect and the doc is unloaded, causing steady RSS growth unrelated to active docs (the old code kept no such map). Secondary correctness edge: a stale entry from before an unload can make a reopened doc whose content coincides with that old hash skip its disk write, since the skip now trusts in-memory state instead of reading the actual file."},
{"file":"/Users/rahloff/projects/Papyr/apps/server/src/routes.ts","line":791,"summary":"github/push skips the network push when commit.committed is false and local HEAD equals lastPushedHead, ignoring changes to the remote made outside Papyr; the map also never drops deleted projects.","failure_scenario":"User pushes once (lastPushedHead[id]=HEAD). Someone force-resets/reverts the remote branch on GitHub while local HEAD is unchanged. The user clicks Push to restore their content: commitAll returns committed:false and head===lastPushedHead, so the handler returns {ok:true, skipped:true} without pushing — the remote stays reverted while the UI reports success. lastPushedHead also retains entries for deleted projects (slow leak)."},
{"file":"/Users/rahloff/projects/Papyr/apps/web/src/components/Auth.tsx","line":78,"summary":"The reset-link effect calls history.replaceState({}, '', window.location.pathname), discarding the entire query string and URL hash, not just the reset_token param.","failure_scenario":"If any sibling query param or fragment is present on the same initial load as ?reset_token=… (e.g. an OAuth error/state param the server redirected back to '/', or a deep-link hash), replaceState to pathname-only wipes it before the app can read it, so that information is lost. Harmless in the pure reset case but destroys co-arriving state."}]</result>
<usage><subagent_tokens>100488</subagent_tokens><tool_uses>21</tool_uses><duration_ms>520126</duration_ms></usage>
</task-notification>