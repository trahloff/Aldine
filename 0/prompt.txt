Check background task brpsv8dl1 (release workflow run 30009543777 for tag v0.2.0 — multi-arch image build, QEMU arm64 TeX Live is slow). If completed successfully: verify GHCR aldine-app/aldine-compiler latest is now multi-arch (docker manifest inspect ghcr.io/trahloff/aldine-app:latest | grep -c arm64, or pull on this arm64 Mac), verify the GitHub Release v0.2.0 exists (gh release view v0.2.0), optionally quick-test the quickstart snippet natively (no emulation) with compose up + health, then down -v. Report to the user in German. If the workflow failed, read the logs (gh run view 30009543777 --log-failed), diagnose, fix, and re-tag if needed.