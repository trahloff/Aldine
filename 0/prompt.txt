<task-notification>
<task-id>a0a1f9d1dd5efdb10</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-502/-Users-rahloff-projects-Papyr/94ff4a6b-9a22-4b9e-ba63-714ccb35a98b/tasks/a0a1f9d1dd5efdb10.output</output-file>
<status>completed</status>
<summary>Agent "Angle E: cache/wrapper correctness" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>All confirmed. Here are the findings.

[{"file":"apps/server/src/db/json.ts","line":49,"summary":"read() caches the `dflt` on ANY readFileSync failure (not just a genuinely-missing file), and the cache entry is permanent for the process lifetime; a transient error therefore turns into permanent empty state, and the next write() persists that empty default over the real file. This is a regression: pre-cache, each read re-hit disk so a transient failure was transient.","failure_scenario":"users.json exists with 500 accounts. Under fd pressure the first process read of users.json throws EMFILE (or EIO/EACCES); catch sets v={} and caches it (line 50). Every later getUser/findUserByEmail now returns empty (sticky cache — the file is never re-read). The next createUser does m={}; m[id]=u; write(), atomically replacing users.json with a single-user file. All 500 accounts are destroyed on disk. Same mechanism wipes sessions.json (mass logout) or connections.json."},
{"file":"apps/server/src/db/json.ts","line":54,"summary":"read() returns the cached object by reference; every mutator (createUser/updateUser/createSession/createReset/setConnection/addUsageSeconds…) mutates that cached object in place BEFORE write() runs. If write() throws (ENOSPC/EROFS/EMFILE on writeFileSync or renameSync), the in-memory cache is left ahead of disk with an unpersisted mutation that survives until restart and gets silently persisted by the next successful write to that file.","failure_scenario":"Registration: createUser passes the uniqueness check, runs m[u.id]=u (line 70, mutates the cached users map), then write() throws ENOSPC → 500 to the client, nothing on disk. The phantom user is now in the cached map. The user retries the same email: the uniqueness guard (line 67) finds the phantom (same email, new id) and throws 'An account with that email already exists', locking them out — yet no account exists on disk and a restart makes the email free again. If a later unrelated createUser write succeeds, the phantom row is resurrected onto disk."},
{"file":"apps/server/src/collab.ts","line":74,"summary":"lastWritten is cleared for LOADED docs when disk changes under them (line 210) and on evict (line 173), but there is no afterUnloadDocument/onDestroy hook, so a doc that Hocuspocus unloads on last-client-disconnect keeps its stale hash. If disk is then changed out-of-band (git pull/merge/reset) while the doc is unloaded, refreshBranchDocsFromDisk skips it (not in the registry), leaving the map hash disagreeing with disk. After reload, a store whose content hashes back to the stale value is wrongly skipped, so the live doc and disk permanently diverge.","failure_scenario":"User edits file f to content X; writeDocToDisk writes X, lastWritten=sha(X). All editors close → Hocuspocus unloads the doc; lastWritten keeps sha(X). User runs a GitHub pull that rewrites f on disk to Y (doc unloaded, so refreshBranchDocsFromDisk skips it). Editor reopens: onLoadDocument seeds the fresh Y.Doc from disk (Y), user undoes the pulled change back to X. onStoreDocument → sha(X)===lastWritten → write SKIPPED. Disk stays Y while the editor shows X; compile and the debounced auto-commit both use the stale disk content, silently discarding the user's edit."},
{"file":"apps/server/src/collab.ts","line":51,"summary":"lastWritten grows unbounded: entries are only ever removed by evictDoc (delete/rename) and refreshBranchDocsFromDisk (loaded docs). A doc that simply unloads because all clients disconnected — the common case — leaves its docName→sha1 entry behind forever, so the map accumulates one entry per (project,branch,file) ever opened over the process lifetime.","failure_scenario":"A long-lived server serving many projects: users open thousands of distinct files over weeks; each open/edit/close cycle adds a permanent lastWritten entry (docName string + 40-char hash) that is never reclaimed after the doc unloads. Slow but monotonic heap growth on a busy multi-tenant node, with no upper bound tied to currently-loaded docs."},
{"file":"apps/server/src/routes.ts","line":50,"summary":"lastPushedHead is never cleared on project delete (the DELETE handler at line 308 calls store.deleteProject but does not remove the map entry), so it leaks one entry per project ever pushed. Secondary: because it records a purely local belief about a remote fact, a remote-side rollback of an already-pushed commit is never re-pushed.","failure_scenario":"Leak: every project that is pushed then deleted leaves a permanent projectId→head entry. Divergence: push succeeds, lastPushedHead[id]=C1, remote=C1; the remote branch is later force-reset on GitHub so it no longer contains C1. The next auto-sync push finds nothing newly committed (committed=false) and head===C1===lastPushedHead[id], so it returns {skipped:true} and never re-pushes C1 — the commit stays missing from the remote until a new local commit or a server restart clears the map."}]</result>
<usage><subagent_tokens>73129</subagent_tokens><tool_uses>18</tool_uses><duration_ms>369065</duration_ms></usage>
</task-notification>